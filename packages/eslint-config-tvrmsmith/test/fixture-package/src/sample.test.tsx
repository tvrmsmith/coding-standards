/**
 * A real file on disk, because `projectService` can only open a case snippet under a path
 * TypeScript's project actually contains. The suite lints its own snippets *as* this path;
 * the content here is only what the clean-lint check sees.
 */
export {}
