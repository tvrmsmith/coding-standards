{
  "name": "coding-standards-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Machine-local adoption harness: the ESLint layering wrapper, the changed-files linter and the pre-commit hook that installs them into a repo without committing anything to it.",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "@eslint/eslintrc": "^3.3.1",
    "@eslint/js": "^9.39.0",
    "eslint-config-tvrmsmith": "link:../packages/eslint-config-tvrmsmith"
  }
}
